localhost {
    tls /certs/localhost.pem /certs/localhost-key.pem {
        protocols tls1.2 tls1.3
    }

    @websockets {
        header Connection *Upgrade*
        header Upgrade websocket
    }
    reverse_proxy @websockets ml-service:8000
    reverse_proxy ml-service:8000
    encode gzip
}